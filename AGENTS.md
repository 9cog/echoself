## Learned User Preferences

- On Windows PowerShell, chain shell commands with `;` rather than `&&`, and pass git commit messages with `-m` rather than bash HEREDOC.

## Learned Workspace Facts

- `C:\hyp\ghx\echoself` is an EchoSelf/Deep Tree Echo checkout paired with sibling repo `C:\hyp\ghx\echoself-1`.
- This Cursor workspace is often a multi-root pairing of those two sibling checkouts; shared agent transcripts live under `c-hyp-ghx-echoself-1`, while the local slug `c-hyp-ghx-echoself` has no `agent-transcripts/` folder yet.
