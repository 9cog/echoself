# Model checkpoints - now tracked via Git LFS
# (*.pt, *.pth, *.bin are handled by root .gitattributes for LFS)

# Keep important metadata and summaries
!**/metadata.json
!**/training_summary.json
!**/cache_stats.json
