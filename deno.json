{
  "tasks": {
    "lint": "deno lint",
    "lint:fix": "deno lint --fix",
    "format": "deno fmt",
    "format:check": "deno fmt --check"
  },
  "lint": {
    "rules": {
      "tags": ["recommended"],
      "exclude": [
        "no-sloppy-imports",
        "jsx-button-has-type",
        "no-window",
        "no-explicit-any",
        "require-await",
        "no-unused-vars"
      ]
    }
  },
  "fmt": {
    "useTabs": false,
    "lineWidth": 80,
    "indentWidth": 2,
    "semiColons": true,
    "singleQuote": false,
    "proseWrap": "preserve"
  },
  "_comment": "Linting rules excluded for gradual migration. TODO: Re-enable rules incrementally: no-sloppy-imports (requires .ts extensions), jsx-button-has-type (130 instances), no-explicit-any (22 instances), require-await (30+ instances), no-unused-vars (7 instances), no-window (2 instances)"
}
